import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Fastship | Dashboard",
};

export default function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  )
}
